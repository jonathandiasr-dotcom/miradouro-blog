The offer / expertise block: line picto left, doré title, 2-4 capability lines.

```jsx
<ExpertiseCard icon="cloud" title="Cloud & DevOps" items={["Audit organisationnel DevOps","Diagnostic d'architectures techniques"]} />
```

Use in a two- or three-column grid with `gap: var(--space-7)`. On bleu marine grounds pass `tone="dark"`.
