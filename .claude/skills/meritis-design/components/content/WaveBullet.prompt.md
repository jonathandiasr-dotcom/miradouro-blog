Section names and important points, bulleted with a small onde instead of a dot (charte p.16).

```jsx
<ul style={{margin:0,padding:0,display:'flex',flexDirection:'column',gap:'var(--space-3)'}}>
  <WaveBullet>Présentation générale</WaveBullet>
  <WaveBullet shape="pleine" color="gold">Cloud &amp; DevOps</WaveBullet>
</ul>
```

Reserve for headline lists. Plain running text keeps classic • bullets.
