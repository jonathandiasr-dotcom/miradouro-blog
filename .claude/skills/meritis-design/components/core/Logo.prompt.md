The Meritis signature, served from `assets/logos/`.

```jsx
<Logo lockup="principal" height={72} />
<Logo lockup="secondaire" tone="white" height={32} />
```

Never recolour, outline, stretch, re-space or re-stack the logo (charte p.6). Keep a clear zone equal to the wordmark's cap height on all four sides.
